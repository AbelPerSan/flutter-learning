import 'package:flutter/material.dart';

void main() {
  runApp(const BanderasApp());
}

class BanderasApp extends StatelessWidget {
  const BanderasApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(

    );
  }
}